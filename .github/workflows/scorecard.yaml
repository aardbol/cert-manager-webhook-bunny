name: OpenSSF Scorecard

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

on:
  push:
    branches:
      - main
    paths:
      - '**/*.go'
      - go.mod
      - go.sum
      - Containerfile
      - '.github/workflows/**'
      - '.golangci.yml'
      - '.trivyignore.yaml'
  schedule:
    - cron: "00 06 * * 1"

permissions:
  contents: read

jobs:
  analysis:
    permissions:
      contents: read
      security-events: write
      id-token: write
    uses: upcloud-tools/gha-actions/.github/workflows/scorecard.yaml@c0df829cbad57268497db8cfb6174419f925b283
    secrets: inherit
